classDiagram
    class Task {
        +String name
        +String time
        +int priority
        +String description
        +String frequency
        +bool completed
        +bool agent_created
        +date due_date
        +/due_in : String
        +edit(name, time, priority, description, frequency)
        +mark_complete()
        +renew()
    }

    class Pet {
        +String name
        +String species
        +String breed
        +int age
        +List~Task~ tasks
        +assign_task(task)
        +get_summary()
    }

    class Scheduler {
        +Owner owner
        +get_all_tasks()
        +generate_plan()
        +filter_tasks(pet_name, completed)
        +detect_conflicts(candidate)
        +sort_by_time()
    }

    class Owner {
        +String name
        +String email
        +List~Pet~ pets
        +Scheduler scheduler
        +register_pet(pet)
        +create_task(name, time, priority, description, frequency)
    }

    class AgentDecision {
        +String rule
        +String action
        +String reasoning
        +String target
    }

    class PawAgent {
        +Owner owner
        +List~AgentDecision~ decisions
        +run() List~AgentDecision~
        +_check_guardrails(pet, task) bool
        +_profile_rule()
        +_coverage_rule()
        +_species_rule()
        +_urgency_rule()
        +_conflict_resolution_rule()
    }

    Owner "1" --> "0..*" Pet : owns
    Owner "1" --> "1" Scheduler : has
    Owner ..> Task : creates
    Pet "1" --> "0..*" Task : stores
    Scheduler --> Owner : references
    Task --> Task : renew()
    PawAgent --> Owner : references
    PawAgent ..> AgentDecision : creates
    PawAgent ..> Task : creates